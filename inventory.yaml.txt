# NOTE: IPs and credentials below are placeholders.
# I will update these later after I set up management VLAN + SSH on my switches.

switches:
  - name: core-2960x
    host: 192.168.99.11         # placeholder mgmt IP for the 2960-X
    device_type: cisco_ios
    username: CHANGE_ME_LATER   # SSH username (will create on switch later)
    password: CHANGE_ME_LATER   # SSH password (will set later)

  - name: access-2960
    host: 192.168.99.12         # placeholder mgmt IP for the 2960
    device_type: cisco_ios
    username: CHANGE_ME_LATER
    password: CHANGE_ME_LATER
